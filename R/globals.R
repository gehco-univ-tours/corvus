globalVariables(unique(c(
  # db_get_validated_period_data:
  "ts_end", "ts_start",
  # db_min_max_date:
  "sensor_id",
  # compile_gb:
  ".data", "Date", "Time", "ts",
  # compile_raw:
  ".data", "data", "time", "ts",
  # db_get_fieldwork_data:
  "ts",
  # db_get_measurement:
  "ts"
)))
